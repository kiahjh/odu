import { expect, it, describe } from "bun:test";

describe(`openDirectoryButtonClicked`, () => {
  it(`should set the fileTree status to loading`, () => {
    // TODO: how to mock the global state?
    expect(true).toBe(true);
  });
});
